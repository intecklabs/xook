name: Release

# Crea una Release en GitHub con el instalador cada vez que se publica una etiqueta vX.Y.Z.
# El instalador se compila sin publicar y después se sube en un solo paso, para que exista
# exactamente una Release por etiqueta (electron-builder --publish puede crear duplicados).
on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run build
      - run: npx electron-builder --win --publish never
      - uses: softprops/action-gh-release@v2
        with:
          name: Xook ${{ github.ref_name }}
          files: |
            dist/*.exe
            dist/*.blockmap
            dist/latest.yml
          fail_on_unmatched_files: true
